一、统一断言封装与设计
（1）通过Excel维护断言数据
（2）设计断言数据编写格式：
// 1、如果是对http响应状态码做断言。“statuscode”:XXX
// 2、如果是响应体字段断言。“jsonpath表达式”:YYY
// 3、如果是响应体文本断言。“bodyString”:ZZZ
（3）通过Java代码解析
思路：
// 1、把读取到Excel断言数据这一列字符串数据(写的时候为字符串--json格式)转为Map--键值对样式
// 2、循环遍历Map，取到里面键和值
// 3、通过if...else...对键做判断，分别去执行不同逻辑
（4）封装为公共方法，放到BaseTest公共类中

二、Allure报告集成与配置